[BUGS]
1. 当在调用模型，人为通过ctrl+c的方式取消时，就无法进行下一步对话（这是因为模型调用的ctx被cancel了）
2. 

[FEATURES]
1. 更美观的控制台输出，能够有有一个类似claude code的界面，显示当前使用模型，claudego的版本，以及所处的项目目录
2. 只显示执行的关键内容（比如调用工具，模型分析）
3. 每个步骤

我发现我目前的这个项目存在很多处的模型调用，工具调用等等重复代码，是否应该将其单独整合出来？此外，我发现每次对话的循环，模型会忘记上一轮对话的内容，我是否应该增加一个简单的memory模块对上下文进行处理（之后我可能会有更进一步的优化，比如上下文压缩）